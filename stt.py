# Stub — implementation owned by task-4-stt (Jai)
# This file exists so main.py can import it; tests mock it.
import numpy as np


def transcribe(audio: np.ndarray, sample_rate: int) -> tuple[str, float]:
    """Returns (text, confidence). Raises NotImplementedError until implemented."""
    raise NotImplementedError("stt.transcribe not yet implemented")
